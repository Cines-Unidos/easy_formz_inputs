name: easy_formz_inputs
description: The easy_formz_inputs library provides pre-built, validated input fields for use in Dart and Flutter applications. Inputs currently provided include email, password, confirm password, urls, date, phone, and non-empty fields.
repository: https://github.com/Cines-Unidos/easy_formz_inputs
issue_tracker: https://github.com/Cines-Unidos/easy_formz_inputs
homepage: https://github.com/Cines-Unidos/easy_formz_inputs
documentation: https://github.com/Cines-Unidos/easy_formz_inputs

version: 0.2.0

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  equatable: ^2.0.5
  formz: 0.8.0
  intl: ^0.20.2
  
dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.16.0
  very_good_analysis: ^9.0.0
